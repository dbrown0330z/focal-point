import type { DisplaySettings } from './actions'

export const DEFAULT_DISPLAY_SETTINGS: DisplaySettings = {
  layout:        'masonry',
  density:       'default',
  spacing:       'standard',
  corners:       'rounded',
  showTitle:     true,
  showScore:     true,
  showMakerName: false,
}
